---

mysql_root_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/mysql_root_password.txt chars=ascii_letters,digits') }}"

pdns_db_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/pdns_db_password.txt chars=ascii_letters,digits') }}"

